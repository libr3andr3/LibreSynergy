// Jitsi Meet web interface configuration
// Generated by libresynergy — config-only, no source modifications

var config = {
    // Connection
    hosts: {
        domain: 'meet.{{ domain }}',
        muc: 'conference.meet.{{ domain }}',
        focus: 'focus.meet.{{ domain }}'
    },

    // BOSH URL for XMPP
    bosh: 'https://meet.{{ domain }}/http-bind',

    // Websocket URL
    websocket: 'wss://meet.{{ domain }}/xmpp-websocket',

    // Client node for XMPP
    clientNode: 'https://jitsi.org/jitsimeet',

    // Authentication
    enableUserRolesBasedOnToken: true,

    // Branding
    brandName: '{{ community_name }}',

    // Features
    disableDeepLinking: false,
    disableThirdPartyRequests: false,
    enableWelcomePage: true,
    enableClosePage: true,
    enableNoisyMicDetection: true,
    enableSaveLogs: false,

    // Recording
    enableRecording: false,
    liveStreamingEnabled: false,

    // Security
    requireDisplayName: true,
    enableInsecureRoomNameWarning: false,

    // Room defaults
    defaultLanguage: 'en',
    startWithAudioMuted: false,
    startWithVideoMuted: false,

    // Toolbar
    toolbarButtons: [
        'microphone', 'camera', 'closedcaptions', 'desktop',
        'fullscreen', 'fodeviceselection', 'hangup',
        'profile', 'chat', 'recording', 'livestreaming',
        'etherpad', 'sharedvideo', 'settings', 'raisehand',
        'videoquality', 'filmstrip', 'invite', 'feedback',
        'stats', 'shortcuts', 'tileview', 'videobackgroundblur',
        'download', 'help', 'mute-everyone', 'security'
    ],

    // JWT
    jwtAppId: 'libresynergy',

    // Analytics
    analytics: {
        disabled: true
    },

    // Deployment info
    deploymentInfo: {
        userRegion: 'default'
    }
};
