# bridge.env — join-bridge (free membership signup)
# Copy to ${LS_SECRETS_DIR}/bridge.env. chmod 600.
# The join-bridge needs no payment secrets: membership is free. Its Authentik
# API token arrives via authentik.env; this file exists for future bridge
# settings and may stay empty.
